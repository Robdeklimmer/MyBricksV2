version: '3.8'

services:
  mysql:
    image: mysql:8.0
    container_name: mybricks-mysql
    environment:
      MYSQL_ROOT_PASSWORD: dev
      MYSQL_DATABASE: mybricks
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
